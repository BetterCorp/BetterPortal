x-bp-build: &bp-build
  context: .
  dockerfile: Dockerfile.coolify

x-bp-common: &bp-common
  image: betterportal:${BP_IMAGE_TAG:-coolify}
  build: *bp-build
  restart: unless-stopped

x-bp-vault-env: &bp-vault-env
  BSB_WRITABLE_PATHS: /data
  BSB_CONFIG_PLUGIN: config-vault
  BSB_CONFIG_PLUGIN_PACKAGE: "@bsb/config-vault"
  BSB_PLUGIN_DIRS: /bp/plugins
  vaultUrl: ${BP_VAULT_URL:-https://vault.eu.core.betterportal.net}

services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${BP_POSTGRES_DB:-betterportal}
      POSTGRES_USER: ${BP_POSTGRES_USER:-betterportal}
      POSTGRES_PASSWORD: ${BP_POSTGRES_PASSWORD:?Set BP_POSTGRES_PASSWORD for BetterPortal Postgres}
    volumes:
      - bp-postgres:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  config-manager:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_CONFIG_MANAGER_VAULT_API_KEY_ID:?Set BP_CONFIG_MANAGER_VAULT_API_KEY_ID for the config-manager service}
      apiSecret: ${BP_CONFIG_MANAGER_VAULT_API_SECRET:?Set BP_CONFIG_MANAGER_VAULT_API_SECRET for the config-manager service}
    volumes:
      - bp-config-manager:/data
    ports:
      - "${BP_CONFIG_MANAGER_PORT:-3300}:${BP_CONFIG_MANAGER_PORT:-3300}"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:${BP_CONFIG_MANAGER_PORT:-3300}/.well-known/bp/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5

  bootstrap1:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_BOOTSTRAP1_VAULT_API_KEY_ID:?Set BP_BOOTSTRAP1_VAULT_API_KEY_ID for the bootstrap1 service}
      apiSecret: ${BP_BOOTSTRAP1_VAULT_API_SECRET:?Set BP_BOOTSTRAP1_VAULT_API_SECRET for the bootstrap1 service}
    volumes:
      - bp-bootstrap1-data:/data
    ports:
      - "${BP_BOOTSTRAP1_PORT:-3100}:${BP_BOOTSTRAP1_PORT:-3100}"
    depends_on:
      - config-manager
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:${BP_BOOTSTRAP1_PORT:-3100}/.well-known/bp/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5

  embedded:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_EMBEDDED_VAULT_API_KEY_ID:?Set BP_EMBEDDED_VAULT_API_KEY_ID for the embedded service}
      apiSecret: ${BP_EMBEDDED_VAULT_API_SECRET:?Set BP_EMBEDDED_VAULT_API_SECRET for the embedded service}
    volumes:
      - bp-embedded-data:/data
    ports:
      - "${BP_EMBEDDED_PORT:-3110}:${BP_EMBEDDED_PORT:-3110}"
    depends_on:
      - config-manager

  auth-default:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_AUTH_DEFAULT_VAULT_API_KEY_ID:?Set BP_AUTH_DEFAULT_VAULT_API_KEY_ID for the auth-default service}
      apiSecret: ${BP_AUTH_DEFAULT_VAULT_API_SECRET:?Set BP_AUTH_DEFAULT_VAULT_API_SECRET for the auth-default service}
    volumes:
      - bp-auth-default-data:/data
    ports:
      - "${BP_AUTH_DEFAULT_PORT:-3210}:${BP_AUTH_DEFAULT_PORT:-3210}"
    depends_on:
      - config-manager

  auth-authress-io:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_AUTHRESS_VAULT_API_KEY_ID:?Set BP_AUTHRESS_VAULT_API_KEY_ID for the auth-authress-io service}
      apiSecret: ${BP_AUTHRESS_VAULT_API_SECRET:?Set BP_AUTHRESS_VAULT_API_SECRET for the auth-authress-io service}
    volumes:
      - bp-authress-data:/data
    ports:
      - "${BP_AUTHRESS_PORT:-3212}:${BP_AUTHRESS_PORT:-3212}"
    depends_on:
      - config-manager

  docs-site:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_DOCS_VAULT_API_KEY_ID:?Set BP_DOCS_VAULT_API_KEY_ID for the docs-site service}
      apiSecret: ${BP_DOCS_VAULT_API_SECRET:?Set BP_DOCS_VAULT_API_SECRET for the docs-site service}
    volumes:
      - bp-docs-data:/data
    ports:
      - "${BP_DOCS_PORT:-3400}:${BP_DOCS_PORT:-3400}"
    depends_on:
      - config-manager

  hello-view:
    <<: *bp-common
    environment:
      <<: *bp-vault-env
      apiKeyId: ${BP_HELLO_VAULT_API_KEY_ID:?Set BP_HELLO_VAULT_API_KEY_ID for the hello-view service}
      apiSecret: ${BP_HELLO_VAULT_API_SECRET:?Set BP_HELLO_VAULT_API_SECRET for the hello-view service}
    volumes:
      - bp-hello-data:/data
    ports:
      - "${BP_HELLO_PORT:-3205}:${BP_HELLO_PORT:-3205}"
    depends_on:
      - config-manager

volumes:
  bp-postgres:
  bp-config-manager:
  bp-bootstrap1-data:
  bp-embedded-data:
  bp-auth-default-data:
  bp-authress-data:
  bp-docs-data:
  bp-hello-data:
