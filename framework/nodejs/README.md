# BetterPortal Framework for Node.js

This package is the BetterPortal v10 core framework for Node.js.

It provides:

- strict platform contracts
- media type negotiation helpers
- view and manifest builders
- tenant/app resolution helpers
- a minimal BSB runtime plugin shell for framework-owned behavior

The framework package does not implement auth, theme, or business logic directly. Those live in separate plugin packages.
