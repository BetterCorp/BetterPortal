# BetterPortal Framework for Node.js

This package is the BetterPortal v10 core library for Node.js.

It provides:

- strict platform contracts
- observability contracts
- media type negotiation helpers
- view and manifest builders
- tenant/app resolution helpers

The framework package does not implement auth, theme, BSB integration, or business logic directly. Those live in separate packages.
